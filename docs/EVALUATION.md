# EVALUATION — pre-registered protocol

**Status: pre-registered. Committed and tagged before the simulator generated
a single seed.**

This document specifies what will be measured, how, against what baselines,
and — most importantly — **what result would count as this project having
failed.** It is written deliberately in ignorance of the numbers. Nothing in
it may be changed after the first holdout run except by the amendment
procedure in §10, which requires the change and its reason to be recorded in
this file rather than silently applied.

The reason for pre-registering is not ceremony. Every design decision in
`docs/taxonomy.md` is an empirical bet, and this system's results are
generated by a simulator I wrote myself. Without a protocol fixed in advance,
"the evaluation" and "tuning until the numbers look good" are the same
activity performed in the same afternoon, and neither I nor a reader could
tell them apart afterwards.

---

## 1. The honest problem with this evaluation

State it first, because everything below is shaped by it.

**I am measuring an agent against a world I built.** The simulator decides
whether a retry succeeds. I wrote the simulator. If I choose an outcome model
generous to timing-aware retries, salary-aware scheduling wins; if I choose
one generous to persistence, it loses. Nothing in the pipeline prevents that,
and no amount of statistical machinery downstream — bootstrap intervals,
stratified splits, `pass^k` — repairs an outcome model chosen to produce a
conclusion. Confidence intervals on a rigged generator are precise
measurements of a decision I already made.

There are exactly three defences, and this protocol uses all three:

1. **Fix the outcome model before running anything**, with every parameter
   carrying an explicit provenance tag (§4). A parameter I invented is
   labelled as invented, in the code as well as here.
2. **Sweep every parameter ±50%** and report which conclusions survive and
   which don't (§7). A conclusion that flips under a plausible parameter
   change is not a finding; it is an artifact, and will be reported and
   dropped as one.
3. **Separate the claims the simulator cannot fake from the claims it
   decides** (§2). This is the strongest of the three and the one most
   evaluations skip.

What this evaluation cannot do is establish real-world recovery rates. It has
no access to real recovery outcomes; `docs/VERIFIED.md` records that only
`payment_failed` is even reproducible against live test mode, and no
successful recovery has ever been observed on this account. **Any number
below describing recovery is a statement about a model, not about India's
payment rails,** and will be labelled that way in the report card and said
aloud in the video.
 
---

## 2. Two kinds of claim

The single most important distinction in this document.

### 2a. Simulator-independent claims (the simulator cannot fake these)

These are properties of the system's own behaviour. They hold or fail
regardless of what outcome model runs underneath, because they are about what
the agent *did*, not whether it worked.

| Claim | Measured by | Passing value |
|---|---|---|
| No message sent outside 08:00–19:00 IST | Ledger scan of every executed comms receipt | **0 violations** |
| No message sent on an unregistered DLT template | Ledger scan | **0** |
| No automated action on a `RISK_BLOCK` episode | Ledger scan | **0** |
| No action after consent withdrawal | Ledger scan | **0** |
| No retry on an `INSTRUMENT_DEAD` classification beyond the documented single probe | Ledger scan | **0** |
| Contacts per episode ≤ 2; per customer per 7 days ≤ 3 | Ledger scan | **0 breaches** |
| Every money action has a hash-chained receipt | `verify_chain` over the full run | **True** |
| Every receipt id unique across the run | Set cardinality vs. count | **Equal** |

Call this conjunction **the safety predicate**. It is referenced throughout.

These are the claims the submission actually rests on, and they are the ones
a hostile reader should attack last, because the ledger is the evidence and
the ledger is reproducible from a seed. **If any of these fails, the project
has failed at its stated purpose** regardless of how good the recovery
numbers look.

### 2b. Simulator-dependent claims (the outcome model decides these)

Recovery rate, rupees recovered, attempts consumed, time-to-recovery. Every
one of these is a function of parameters I chose. They are reported with
intervals, swept for sensitivity, and stated as conditional on the model —
never as a claim about production.

The report card renders these two groups in visually distinct sections. A
reader must not have to work out which is which.

---

## 3. Design

### 3a. Randomisation unit: the customer

Episodes from the same customer are not independent. They share a payment
instrument, a consent record, a DND status, a contact history, and a
frequency-cap budget — and `FrequencyCapGuard` and `PromiseToPayGuard`
explicitly couple them. Splitting at the episode level would leak: the same
customer's earlier episode would inform the treatment of their later one
across the split boundary, and the cap state would be shared across a
division that is supposed to be independent.

**Randomisation is therefore at the customer level.** A customer and all
their episodes land wholly on one side of the split.

### 3b. Stratification

The failure-class distribution is not uniform, and the classes differ
enormously in recoverability (`LIQUIDITY` is the most recoverable;
`RISK_BLOCK` is unrecoverable by construction). An unstratified split can
hand one side a materially easier population by chance.

Customers are stratified by **their first episode's failure class** (five
strata) before assignment. Within stratum, assignment is by a seeded
permutation.

### 3c. Split — and what it does *not* do

- **Development set: 40%** of customers. Everything I look at while
  iterating comes from here.
- **Holdout: 60%**, sealed until the protocol below is executed once.

**Be precise about what this buys.** There is no fitted model here. The
classifier is a deterministic dictionary; nothing is trained, so this is not
a train/test split in the machine-learning sense and it does not protect
against overfitting in that sense. What it protects against is narrower and
still real: **me tuning thresholds — retry counts, the salary-window bounds,
guard parameters — until the numbers improve, and then reporting those
numbers as though the thresholds had been chosen in advance.** The taxonomy
contains at least four such knobs (§6's twelve- and twenty-day caps,
`card_declined`'s single probe, the per-episode contact cap), each currently
justified by argument rather than data, and each an obvious candidate for
exactly that kind of drift.

The holdout is evaluated **once**. If a bug is found after the holdout run
that invalidates it, the fix and the re-run are both recorded in §10 with the
reason. A silent re-run is the failure mode this rule exists to prevent.

### 3d. Universe

500 customers, seeded, with failure reasons drawn from a fixed distribution
registered here.

No source publishes an Indian card-decline mix at the `error_reason`
granularity `docs/taxonomy.md` uses. Rather than fabricate a citation or
default to a uniform mix that no real merchant has ever seen, the
distribution below is registered as **`[guess]`**, shaped so that every one
of the five failure classes is exercised and the generic case dominates —
which is the one thing `docs/VERIFIED.md` does establish, since generic
`payment_failed` is the only reason live test mode ever produced.

| `error_reason` | Share | Class |
|---|---|---|
| `payment_failed` (gateway / bank / business split 70/25/5) | **0.30** | TRANSIENT / INSTRUMENT_DEAD / RISK_BLOCK |
| `insufficient_fund` | **0.22** | LIQUIDITY |
| `card_declined` | **0.12** | INSTRUMENT_DEAD |
| `gateway_technical_error` | **0.10** | TRANSIENT |
| `payment_timed_out` | **0.08** | TRANSIENT |
| `payment_cancelled` | **0.07** | CUSTOMER_ACTION |
| `card_expired` | **0.05** | INSTRUMENT_DEAD |
| `card_disabled_for_online_payments` | **0.03** | INSTRUMENT_DEAD |
| `card_number_invalid` | **0.02** | CUSTOMER_ACTION |
| `payment_risk_check_failed` | **0.01** | RISK_BLOCK |

Two consequences a reader should hold onto. **The mix decides the headline
recovery rate**, because the classes differ enormously in recoverability —
weight `LIQUIDITY` up and every arm improves. And `card_expired` at 0.05
means the flagship claim rests on roughly one episode in twenty, so F2's
interval will be wider than the others; that is a property of the registered
mix, not a weakness discovered afterwards.

The mix is swept under §7 like any other registered parameter.

---

## 4. The outcome model — where this is most attackable

The outcome model answers one question: *given an intervention on an episode
in state X at time T, does money arrive?*

Every parameter is fixed here, before any run, and carries a provenance tag
**in the simulator's own source, not only in this document**. A parameter
with no tag fails a test.

- `[cited]` — traced to a named external source, recorded in
  `docs/VERIFIED.md`
- `[derived]` — computed from a `[cited]` figure by stated arithmetic
- `[guess]` — my judgement, with no external support

| Parameter | Registered value | Provenance |
|---|---|---|
| P(success \| `SILENT_RETRY` on `TRANSIENT`, attempt 1) | **0.35** | `[guess]` |
| P(success \| `TIMED_RETRY` on `LIQUIDITY`, in salary window) | **0.55** | `[guess]` |
| P(success \| `TIMED_RETRY` on `LIQUIDITY`, outside window) | **0.15** | `[guess]` |
| P(success \| retry on `INSTRUMENT_DEAD`) | **0.0** | `[derived]` — definitional; taxonomy §5 |
| P(customer completes `REAUTH_LINK` \| delivered, in-window) | **0.25** | `[guess]` |
| P(customer completes `REATTEMPT_LINK` \| delivered) | **0.35** | `[guess]` |
| P(out-of-band settlement, per episode-day) | **0.02** | `[guess]` |
| Salary-window balance uplift multiplier | **2.0×** | `[guess]` |

**Eight of the nine parameters are `[guess]`.** The eight rows above are
seven guesses and one definitional zero; the ninth,
`retry_success_unpriced_class`, was added under §10 after this table was
registered and is a guess as well. Conditional retry-success probabilities at
this granularity are not published by anyone — not by NPCI, not by the
PA/PGs, not in any paper I could find — and inventing a citation for them
would be the first dishonest sentence in this repository. So they are my
judgement, labelled as my judgement, everywhere they appear. The one non-guess
is definitional rather than measured: an expired card authorising is not a
low-probability event, it is not an event.

**The reasoning behind each guess**, so a reader can attack the reasoning
rather than the number:

- **0.35 on a transient retry** — gateway blips clear on their own; roughly a
  third clearing within one backoff step is defensible and deliberately not
  generous.
- **0.55 in-window vs 0.15 outside** — this pair is the most consequential
  guess in the file, because the gap between them *is* taxonomy §6's
  salary-timing hypothesis expressed as a parameter. A2's result is largely
  determined by it. If the true gap is smaller, §6 is a smaller effect than
  the point estimate will suggest, which is precisely what the ±50% sweep
  exists to expose. **Treat A2's headline number as untrustworthy until §7
  reports on it.**
- **0.25 on a re-auth link** — completion requires re-entering full card
  details on a page arriving unprompted; friction is high and trust is low.
- **0.35 on a re-attempt link** — same flow, less friction: the instrument
  already works, so this is a retry the customer chose rather than a new
  instrument they must supply.
- **0.02 out-of-band per episode-day** — rare but non-zero. This is the
  parameter that drives A07 (out-of-band settlement) into the run at all; set
  it to zero and the attack never fires in evaluation.
- **2.0× uplift** — a round number, chosen because it is arguable in both
  directions rather than because anything supports it.

**The `[guess]` fraction in the outcome model — 8/9 — is itself a headline
result** and appears in the report card as prominently as the recovery rate.
The recovery numbers substantially describe my priors, and a reader is
entitled to know that without opening the source.

**One asymmetry I am committing to now:** where a parameter choice would
plausibly favour Vasool over a baseline that does *not* share it, I take the
value least favourable to Vasool within the plausible range. Where a
parameter affects all arms identically the rule does not apply and no claim
of conservatism is made for it.

---

## 5. Baselines

Vasool is compared against three, all running on the identical seeded
universe with identical arrivals and identical outcome draws:

1. **`naive_retry`** — the strawman worth beating: retry every failure on
   fixed exponential backoff (5m/30m/4h) until the attempt cap, regardless of
   reason. No classification, no guards, no contact.
2. **`retry_plus_contact`** — the realistic incumbent: `naive_retry` plus a
   payment link after the retries exhaust. No taxonomy, no compliance layer.
   **This is the baseline that matters**; beating `naive_retry` proves very
   little.
3. **`vasool_ungated`** — Vasool's full taxonomy and timing with the guard
   chain removed. Isolates the recovery cost of compliance. Expected to
   *beat* full Vasool on raw recovery; the gap is the price of the guards and
   is reported as a headline number rather than hidden.

That third baseline is deliberately adversarial to my own thesis. If the
guards cost nothing, the compliance layer is not doing anything and the
thirteen-guard claim is decorative.

---

## 6. Metrics and inference

**Primary:** recovery rate — episodes reaching `RECOVERED` ÷ total episodes,
per arm.

**Secondary:**
- Rupees recovered (`amount_recovered_paise`, summed from the ledger)
- Attempts consumed per successful recovery
- Contacts sent per episode
- Median and p90 time-to-recovery
- Episodes terminating in `ESCALATED` (the human cost of the policy)

**Safety:** the §2a predicate. Every entry must be 0 or True.

### 6a. Paired inference

Every arm runs on the **same seeded universe**: same customers, same
arrivals, same outcome draws. The arms are therefore paired by seed, and
comparing marginal intervals throws that structure away.

**All comparisons are on the per-seed difference.** For each seed *s*,
compute `d_s = metric(Vasool, s) − metric(baseline, s)`; bootstrap the *mean
of d* by resampling seeds; report the 95% percentile interval of the
difference. **Superiority is claimed iff that interval excludes zero.**

Comparing non-overlapping marginal intervals — the obvious version of this —
is a conservative test that would miss real differences the pairing can
detect, and would let me report "no detectable difference" for effects that
are plainly there. It is not used.

**Uncertainty:** bootstrap over 1000 seeds, seed range fixed now at `0..999`.
Within a seed, resampling for any per-customer statistic is at the **customer
level**, matching §3a.

### 6b. `pass^k` — over seeds, not over repeats

`pass^k` is the probability that the §2a safety predicate holds
**simultaneously across k independently seeded universes**, estimated as the
fraction of size-k seed subsets in which every run passes.

The distinction matters and is easy to get wrong. Re-running a *single* seed
is not an independent trial: architectural invariant 5 makes the ledger
byte-identical, so `pass^k` over repeats of one seed is 1.0 by construction
and measures nothing. What varies across *seeds* is the world — which
customers exist, what fails, when it fails, whether an attempt lands at 18:59
or 19:01. A compliance bug that only manifests in an unusual world is exactly
what this is for, and it is discoverable only across seeds.

Reported for k ∈ {1, 5, 10, 25, 50, 100}. A system that satisfies the safety
predicate in 99 of 100 worlds is not safe; `pass^k` makes intermittent
violations visible where a mean would bury them.

**Determinism is checked separately** and is not a `pass^k` question: two runs
of one seed must produce byte-identical ledgers (SHA-256 compared). That is
invariant 5, reported as its own line.

---

## 7. Sensitivity

Every parameter in §4 is swept independently at **−50%, −25%, +25%, +50%** of
its registered value, holding others fixed.

For each conclusion in §9, the report card states: **survives all sweeps /
survives some / flips**. A conclusion that flips under any single ±50% sweep
is reported as an artifact of the outcome model and **is removed from the
pitch**, whatever it says.

Given §4's expectation that most parameters are `[guess]`, this section — not
the point estimates — is where the evaluation's credibility actually lives.
It is also the section that decides whether §1's honesty is real or
performative.

---

## 8. Ablations

Five, each removing exactly one mechanism, all else identical:

| # | Ablation | Tests |
|---|---|---|
| A1 | **No taxonomy** — every failure treated as `TRANSIENT` | Whether classification does anything at all. If A1 matches full Vasool, `docs/taxonomy.md` is decoration. |
| A2 | **No salary-aware timing** — `LIQUIDITY` uses fixed backoff | Taxonomy §6, the argument most specific to India. |
| A3 | **No zero-retry rule** — `INSTRUMENT_DEAD` retries like anything else | The flagship claim: that futile retries cost a budget the re-auth link needed. |
| A4 | **Guard chain short-circuits on first refusal**, in the original spec's cheapest-first order | Session 3's own correction — whether all-then-resolve-by-severity actually mattered, or whether I fixed a bug that never fired. |
| A5 | **No escalation** — retries exhaust and the episode stops, with no link | Whether the "then a link" half of the taxonomy's escalation rows earns its place, or whether the retries do all the work. |

A5 replaces an earlier candidate (removing `RISK_BLOCK`), which was cut
because A1 already routes risk declines as `TRANSIENT` — the two would have
measured overlapping things and one of the five slots would have been wasted.

**The `RISK_BLOCK` rule is deliberately not ablated.** It was never justified
on recovery grounds — taxonomy §2 defends it on four harm-based arguments —
so measuring its recovery cost would answer a question nobody asked and
invite the answer "then remove it." If asked directly, the honest response is
that removing it would likely *raise* measured recovery in this simulator and
the rule stays anyway, for reasons the simulator cannot see.

---

## 9. Falsification — what would make this project wrong

Registered in advance, with thresholds. If a criterion fires, it goes in
`POSTMORTEM.md` and is stated in the video.

**F1 — The taxonomy adds nothing.** If the paired-difference interval for
recovery rate against `retry_plus_contact` includes zero, classification
bought no recovery, and the honest framing becomes *"the taxonomy is a
compliance and cost artifact, not a recovery improvement."*

**F2 — The flagship claim doesn't hold.** If A3's paired-difference interval
includes zero on both recovery rate and attempts-consumed, the `card_expired`
argument is theoretically sound but empirically inert, and the video's
thirty-second centrepiece must say so.

**F3 — Salary timing is noise.** If A2's paired-difference interval includes
zero, taxonomy §6 is an elegant argument with no measured effect, and is
presented as a hypothesis rather than a result.

**F4 — The guards are unreliable.** If `pass^100` < 1.0 on the §2a predicate,
the compliance layer is not dependable across worlds, and no recovery number
matters. **This is the criterion that would end the submission's central
claim.**

**F5 — Compliance is unaffordable.** If `vasool_ungated` beats full Vasool by
more than 20 percentage points of relative recovery, the guard chain as tuned
is not a viable product trade-off, and the correct finding is that some guard
parameters need loosening — not that compliance is optional. (The 20-point
threshold is a judgement, registered now so it cannot be chosen after seeing
the gap.)

**F6 — The conclusions are model artifacts.** If more than half the
conclusions in the report card flip under some ±50% sweep, the evaluation
does not support conclusions and is presented as an instrument-building
exercise instead.

**F7 — Determinism fails.** If two runs on one seed produce different
ledgers, architectural invariant 5 is violated and every number above is
unreproducible.

**F6 is evaluated on the primary metric alone.** §7's grid computes paired
differences on recovery rate and nothing else, so F6 asks whether the seven
primary comparisons survive and cannot see a conclusion that flips only on a
secondary. Attempts-consumed is the case that matters: it is half of F2's own
criterion, so the flagship claim is falsifiable under F2 on the 1000-seed
comparison but its robustness to ±50% on the outcome model is untested on the
metric carrying half of it. A3's recovery-rate comparison is in F6's
denominator; F2 as registered — which requires both metrics — is not evaluated
by F6 at all. Registered 2026-08-24 (§10).

I expect F1 and F4 to pass and I am genuinely unsure about F3 and F5. **F3
and F5 are the two I would most like to be wrong about, which is why they are
written down before I can see them.**

---

## 10. Amendments

This document is append-only after its first commit. Any change is recorded
below with date, what changed, and why — including changes made before any
run. An amendment is marked **POST-HOC** when it was made with the
relevant output visible — the holdout having been inspected is one case of
that, not the only one — and every conclusion depending on it is labelled
accordingly in the report card. This standard supersedes the narrower one it
replaces for every row below, retroactively (§10, 2026-08-24).

| Date | Change | Reason | Post-hoc? |
|---|---|---|---|
| 2026-08-23 | §7 sweeps run on seeds `0..199` (200 seeds), not `0..999`. Survival is judged against an unswept reference recomputed on the same 200 seeds. | §7 registers no seed count of its own — §6a's `0..999` is scoped to the primary inference — so this registers something §7 left open rather than amending §6a. The full grid at 1000 seeds is 24 knobs × 4 points plus 3 mix composites, × 9 arms: roughly 82 hours measured, against ~40 minutes for the base protocol. 200 seeds brings it to one overnight run. The mitigation is not optional: a 200-seed bootstrap interval is about 2.24× wider, so a conclusion could cross zero from lost power rather than from the parameter, and F6 fires on flips. An equally-powered reference makes "flips" a statement about the parameter again. | **Yes** — re-marked 2026-08-24 under §10's superseding POST-HOC standard: this row quotes measured runtime ("roughly 82 hours" against "~40 minutes for the base protocol"), so a measurement chose the seed count, though the measurement was of time and not of any effect. No value, threshold, or conclusion changed. |
| 2026-08-23 | `retry_success_instrument_dead` is **not swept**. Its four §7 configurations are not run; the report card states it as inert-by-construction and shows the arithmetic. | The registered value is 0.0 and every §7 factor is multiplicative: 0.0 × 0.5 = 0.0 × 1.5 = 0.0. Four runs identical to the unswept run would be theatre, and counting them as passed sweeps would overstate how much of the outcome model has actually been tested. §4's one non-guess is definitional, and a definition does not have a sensitivity. | No |
| 2026-08-23 | §3d's mix is swept as **three registered composite configurations**, not as 13 per-share knobs. Each names one or more reasons, scales their registered shares by a §7 factor, and scales every unnamed share by a single common factor so the table sums to 1.0 exactly. **M1 `recoverable_heavy`**: `insufficient_fund` ×1.5; `card_declined`, `card_expired`, `card_disabled_for_online_payments`, `payment_risk_check_failed` ×0.5. **M2 `recoverable_light`**: the mirror — `insufficient_fund` ×0.5, those four ×1.5. **M3 `generic_skews_dead`**: `PAYMENT_FAILED_SOURCE_MIX`'s `bank` ×1.5, with `gateway` and `business` renormalised. | §3d's own sentence is singular — "the mix is swept under §7 like any other registered parameter" — and a per-share sweep has no defined meaning anyway: `windtunnel/rng.py::choose` raises unless shares sum to 1.0 exactly, and §7 registers no renormalisation rule, so "scale by ±50%" is undefined on a simplex without one. Three composites test what §3d says is actually at risk — "weight LIQUIDITY up and every arm improves" — in both directions, plus the source split that makes the single largest reason, 0.30 of all episodes, resolve to three different failure classes. The factors are §7's own ±50%, so no new magnitude enters. | No |
| 2026-08-23 | §8's three under-specified ablations get registered values. **A1** treats every failure as `payment_failed`/`gateway`'s row — one silent retry, then a re-attempt link — rather than `gateway_technical_error`'s three. **A2** uses `TRANSIENT_BACKOFF` (5m → 30m → 4h) for `LIQUIDITY`. **A3** gives each `INSTRUMENT_DEAD` row the retry budget its class would otherwise get: `card_expired` and `card_disabled_for_online_payments` take `payment_failed`/`gateway`'s single probe, and `card_declined` keeps the one it already has. | A1 is "no taxonomy", and an agent with no classification has no basis for knowing a gateway problem is a gateway problem — handing it the informed row's three-retry budget would give it knowledge the ablation is supposed to remove. A2's backoff is already registered and is the one §5.1 names, so no new number enters. A3 removes the zero-retry *rule*, not the budget discipline: three retries on a dead card would test a strawman, while the claim under test is that **one** futile retry costs an attempt the re-auth link needed. | No |
| 2026-08-23 | §9's F5 threshold reads as **20 absolute percentage points** of recovery rate, not 20% relative. | A relative figure inflates as the base rate falls, which would make the guards look most expensive exactly where there was least to lose. Absolute points are what a merchant experiences, and what F5 asks is what compliance costs. **Disclosure:** figures were visible when this reading was fixed — seed 0, 35.7% gated against 52.3% ungated, which is 16.6pp absolute and 46.5% relative, so the two readings gave opposite verdicts on that seed. They were whole-universe figures, not development-set ones, because the §3c split did not exist when that feasibility probe ran; one aggregate recovery rate on one seed is not a holdout evaluation, but it was not holdout-excluded either. The reading registered here is the one that makes F5 **harder** to fire. | **Yes** — re-marked 2026-08-24 under §10's superseding POST-HOC standard. No value, threshold, or conclusion changed. **This row was already disclosing under the stricter standard before that standard was registered** — it names the seed-0 figures, both readings, the fact that they gave opposite verdicts on that seed, and that the reading it registered is the one making F5 harder to fire. The re-mark adds no disclosure the row did not already contain; it makes the header field agree with a paragraph that was honest before there was a rule requiring it to be. |
| 2026-08-23 | §4 prose: "seven of eight" → "eight of the nine", and the headline `[guess]` fraction 7/8 → 8/9, scoped to the outcome model. Cut a paragraph duplicating the one above it. No registered value or tag changed; the §4 table still has eight rows. | The fraction is the report card's most prominent honesty claim and `retry_success_unpriced_class` made it understate the guessing by one parameter. §10's twelve world-shape parameters are guesses too and are outside this fraction, which is why it now names its scope. | No |
| 2026-08-23 | §11: time-to-recovery excludes customer response latency | Settlement lands in the same tick as the action that earned it. No click-delay parameter is registered in §4 and inventing one would be a parameter this protocol never agreed to. | No |
| 2026-08-23 | §4 add: `retry_success_unpriced_class` = 0.35 `[guess]`. Swept. | §4 prices no retry against CUSTOMER_ACTION or RISK_BLOCK because Vasool never retries either; `naive_retry` and A1 do. Set to the highest registered retry rate — generous is the anti-Vasool direction, and zero would flatter Vasool by construction. | No |
| 2026-08-23 | `episodes_per_customer_lambda` = 1.0 `[guess]` (episodes = 1 + Poisson(λ), truncated at 6). Swept. | ~63% of customers get more than one episode. §3a's randomisation argument is that customers share a frequency-cap budget; a one-episode-each universe makes that decoration and `FrequencyCapGuard` unreachable. | No |
| 2026-08-23 | `episode_arrival_window_days` = 60 `[guess]`. Swept. | Two months so both halves of taxonomy §6's salary cycle are exercised — two month-ends and two 1st–7th windows — rather than one payday deciding the run. | No |
| 2026-08-23 | `inter_episode_gap_mean_days` = 9.0 `[guess]` (exponential). Swept. | Puts a little over half of consecutive episode pairs inside `FrequencyCapGuard`'s 7-day window. Never triggering leaves the cap untested; always triggering makes every second episode a block. | No |
| 2026-08-23 | `settlement_drain_days` = 45 `[guess]`. Swept. | Covers the longest trajectory the agent can schedule (§6's three-rung ladder plus escalation, ~40 days) so nothing counts as unrecovered because the simulation stopped. | No |
| 2026-08-23 | `amount_median_rupees` = 1200, `amount_sigma_log` = 1.4 `[guess]` (log-normal). Both swept. | Every payload in `data/` is ₹500 from one checkout. A constant amount leaves `AFAThresholdGuard`, `HumanApprovalGuard` and `SpendCapGuard` permanently unexercised — the report card would show three of thirteen passing having never run. σ sized so a few percent clear ₹15,000 and a fraction of a percent clear ₹50,000. | No |
| 2026-08-23 | `consent_on_file_rate` = 0.97 `[guess]`. Swept, clamped to [0,1]. | The remainder have no record at all, which fails closed rather than permissively (`ConsentGuard.requires`). A world where consent is always present never exercises the guard the DPDP claim rests on. | No |
| 2026-08-23 | `consent_withdrawn_rate` = 0.02 `[guess]`. Swept, clamped. | Puts A12 and §2a's "no action after consent withdrawal" into the run at all. A safety predicate nothing tests is not evidence. | No |
| 2026-08-23 | `dnd_listed_rate` = 0.08 `[guess]`. Swept, clamped. | Inert today and registered anyway: `DNDGuard.applies_to` is PROMOTIONAL-only and every message here is TRANSACTIONAL, so the guard is NOT_APPLICABLE throughout. `proposal.py`'s own VERIFY says that categorisation is unsettled; if it moves, this becomes load-bearing overnight. | No |
| 2026-08-23 | `mandate_share` = 0.35 `[guess]`. Swept, clamped. | Decides `RetryCapGuard`'s ceiling (4 vs 3), whether `PreDebitNoticeGuard` applies, and whether `AFAThresholdGuard` applies. Unanchored in a way the others are not — no subscription payload has ever been observed. | No |
| 2026-08-23 | `promise_to_pay_rate` = 0.05, `promise_horizon_days` = 10 `[guess]`. Both swept. | `PromiseToPayGuard` is one of the two guards §3a names as coupling a customer's episodes. Small because a promise needs an interaction most failures never produce. | No |
| 2026-08-23 | Universe epoch = 2026-09-01 00:00 IST; merchants = 1; consent granted 365d pre-epoch. **Not swept.** | Flagged rather than silently exempted from §7. Scaling a calendar anchor or a count by ±50% is meaningless; the arrival window is swept in the epoch's place. Consent age is inert — `ConsentRecord.covers` has no expiry rule — so sweeping it is guaranteed to show nothing. | No |
| 2026-08-24 | §9's **F6 gets a registered evaluation rule**, which §7 and §9 between them left undefined. **Denominator:** the seven per-arm paired comparisons that a swept parameter can move — `naive_retry`, `retry_plus_contact`, `vasool_ungated`, A1, A2, A3, A5, each against Vasool on §6's primary. Not F1–F7: F6 is self-referential, F7 cannot be moved by a parameter, and F4 and F5 are not paired differences, so `inference.survives` cannot express them. **A4 is excluded**, and the exclusion is registered rather than left implicit: its per-seed difference is identically `[0, 0]`, so its reference interval cannot exclude zero and it fails to survive in every configuration for a reason no swept parameter touches. **Threshold:** F6 fires when **4 or more of the 7** fail to survive — §9's "more than half", applied to that denominator. **Quantifier:** a comparison counts as flipped if it fails to survive in **at least one** configuration, per §9's "under some ±50% sweep". **Grid:** "±50% sweep" means the **whole grid** — all 83 configurations, 20 knobs × 4 points plus 3 mix composites — not the 43 at the ±50% endpoints. **Metric:** the primary alone; F2's second metric, attempts-consumed, is not in the grid, and F6 does not evaluate F2 as registered (stated as a limit in §9). **Flip:** `not survives` counts as flipped. | **These readings were fixed after the 83-configuration grid had been run and its output read, and that is the drift §3c exists to prevent** — a rule chosen once the numbers are visible is a rule the numbers can have chosen. It is registered anyway, because F6 was reporting `null` on runs that included the grid, and a criterion that cannot be evaluated is worse than one registered late; but the disclosure belongs in the row and not in a footnote. **What was visible:** all 83 blocks carried an identical set of survival booleans — A4 false, the other seven true — so F6 did not fire under any of the candidate rules, and the choice between them changed no verdict on this data. **On excluding A4.** It spends a denominator slot before any sweep runs, and it makes F6 — a criterion about *model artifacts* — partly triggered by an arm with a zero-difference vector, which is hostile but meaningless. **≥4 of 7 is the same effective threshold the eight-arm version computed to in practice:** A4 supplied one flip for free, so "≥5 of 8" also required four real flips. The exclusion removes an artifact without making F6 harder to fire, which is what distinguishes it from a rescue. **Where the remaining forks landed — two against Vasool, one in its favour, and the denominator deliberately neutral.** (i) *Grid — against.* Reading "±50% sweep" as the whole grid rather than its endpoints gives every conclusion 83 chances to flip instead of 43. (ii) *Flip — against.* Counting `not survives` as flipped is **a mapping, not a restatement**: F1 and F3 register on *the interval including zero*, whereas `inference.survives` additionally requires the swept interval to agree in sign with the reference **and** requires the reference to have been conclusive at all. It is a strictly broader flip condition than the criteria's own wording — it fires on a sign reversal that still excludes zero, and on a reference that goes inconclusive under a configuration, neither of which F1 or F3 covers. All seven references are conclusive on this data, so the second case is latent rather than active. (iii) *Metric — in Vasool's favour, and not dressed up as anything else.* Restricting F6 to the primary could not be made hostile without adding a second metric to the grid, which changes what §7 computes rather than reading what it already says. The cost is that a conclusion flipping only on a secondary is invisible to F6; §9 now says so. (iv) *Denominator — neutral by construction, and broader than a literal §9 reading.* "Each conclusion in §9" would support only three comparisons here — F1's `retry_plus_contact`, F2's A3, F3's A2 — and the registered seven put four arms under F6 that §9 never named as conclusions, at a fraction (4 of 7) lower than three would have required (2 of 3). Recorded so the breadth is on the record, not claimed as a hostile choice. | **Yes** — fixed after the §7 grid was run and its output read. §10's header ties POST-HOC to holdout inspection and the holdout is unopened (§3c); this row is marked under the broader standard, that the change was made with the relevant output visible. Every conclusion depending on it is F6 itself. |
| 2026-08-24 | **§7's sweep block now carries the paired point estimate and 95% interval per arm per configuration**, beside the `survives` boolean it already reported. `survives` is unchanged — same function, same inputs, byte-for-byte the same values on all 83 configurations. No registered value, threshold, arm definition, outcome-model parameter, or comparison changed: this amends what the manifest **retains**, not what it computes. A4's entry additionally carries the note "sign test undefined for a zero-difference arm; excludes_zero is false by definition." | **The block was reporting a single sign bit and discarding the effect size.** Across all 83 configurations the eight survival booleans were byte-identical — **one** distinct block in the entire grid — which reads as a section that tested nothing. The same 83 configurations expand to **75** distinct blocks once the interval is carried: the swept parameters were moving the effect sizes the whole time, and §7's own output was the one place that could not show it. §7 says this section is where the evaluation's credibility actually lives, and a boolean cannot separate a conclusion that barely held from one no parameter came close to moving — which is the artifact-versus-result question §7 exists to answer. Two things the boolean was hiding: `retry_plus_contact` reports `survives: true` at a reference point estimate of **−0.311**, so what survives every sweep is the conclusion that Vasool is 31 percentage points *behind* it; and A4 reports `survives: false` in every configuration for a reason that has nothing to do with any swept parameter — the observation the row above acts on. **This was a reporting defect in a pre-registered analysis, found by reading my own output rather than by a test** — no test asserted the block's shape, and none would have caught it, because every value in it was correct. Recorded as an amendment rather than left to a commit message because §7's registered verdict changed meaning: "survives all sweeps" is now a statement about effect sizes and not about signs. | **Yes** — the defect was found by reading the grid's output, so the change was made with that output visible, and it is marked under the same standard as the row above. It changes no computed value and no registered quantity, so the set of conclusions depending on it is empty; the field says Yes because of when and how the change arose, not because a number moved. |
| 2026-08-24 | §10's **POST-HOC definition is amended and applied retroactively**. The header sentence becomes: *an amendment is marked* **POST-HOC** *when it was made with the relevant output visible — the holdout having been inspected is one case of that, not the only one — and every conclusion depending on it is labelled accordingly in the report card.* **This supersedes the narrow definition for every row in this table, including the nineteen written before today.** Two rows are re-marked **No → Yes** under it: the **F5 threshold** row (2026-08-23), whose own text already discloses that seed-0 figures were visible when the reading was fixed; and the **§7 seed-count** row (2026-08-23), which quotes measured runtime — "roughly 82 hours" against "~40 minutes for the base protocol" — so a measurement chose the seed count, even though the measurement was of time and not of any effect. Both carry the note: *Re-marked 2026-08-24 under §10's superseding POST-HOC standard. No value, threshold, or conclusion changed.* The F5 row carries one sentence more: **it was already disclosing under the stricter standard before that standard was registered** — it names the seed-0 figures (35.7% gated against 52.3% ungated), both readings (16.6pp absolute, 46.5% relative), the fact that the two gave opposite verdicts on that seed, and that the reading it registered is the one making F5 *harder* to fire. The re-mark adds no disclosure the row did not already contain; it makes the header field agree with a paragraph that was honest before there was a rule requiring it to be. The remaining **seventeen** rows are audited against the same standard and **stay No**, on the basis set out opposite. **Separately, §7's removal rule is registered as a precondition on pitching, and §7's own sentence is left exactly as written.** §7 says a conclusion that flips under any single ±50% sweep *is removed from the pitch, whatever it says*; that sentence stands. What is registered here is the form the rule takes now that F6 has a denominator of its own: **any conclusion pitched must have survived its own comparison.** §7's removal reaches the three comparisons §9's criteria map onto — F1's `retry_plus_contact`, F2's A3, F3's A2 — and F6's threshold reaches all seven; stating the removal rule as a precondition rather than as a set makes that difference harmless. | **The column carried two meanings at once, and the narrower one was the flattering one.** Yesterday's rows were marked against "after the holdout has been inspected"; today's two are marked against "made with the relevant output visible", and the holdout is unopened. A reader meeting seventeen No rows and three Yes rows would reasonably infer the No rows were written blind, and at least one — the F5 threshold, which fixed a reading with figures on screen that gave opposite verdicts — was not. Mixed is the worst of the three available states: it is the one that misleads without a single false sentence in it. **The standard registered here is strictly stricter than the one it replaces, and it is applied backwards over my own earlier rows. That is the entire claim, and it is not a large one** — casting a wider net over work already done is the minimum the document's own premise requires, not a credit to it. **The audit.** Each row was assessed against its own stated justification — what the row says decided it. The seventeen that stay No rest on one of four things, none of which is run output: arithmetic on an already-registered value (`retry_success_instrument_dead`, 0.0 × any factor; §4's 7/8 → 8/9 recount), a property of the code (`rng.py::choose` refusing a table that does not sum to 1.0 exactly; `DNDGuard.applies_to` being PROMOTIONAL-only; `RetryCapGuard`'s 4-vs-3 ceiling), a reading of the protocol's own text (§3d's singular sentence on the mix; §8's ablation semantics; §11's click-delay disclosure), or a closed-form property of a registered distribution. The last deserves naming, because those rows quote figures that read as measurements and are not: `episodes_per_customer_lambda`'s "~63% of customers get more than one episode" is 1 − e⁻¹ = 63.2%; `inter_episode_gap_mean_days`' "a little over half of consecutive episode pairs" is 1 − e^(−7/9) = 54.1%; `amount_sigma_log`'s "a few percent clear ₹15,000 and a fraction of a percent clear ₹50,000" is 3.56% and 0.39% under log-normal(median 1200, σ 1.4); and the §7 row's own "2.24× wider" is √5. Each reproduces from the registered value alone, with no run behind it. They stay No because they were derivable without output, not because re-marking them would have been tidier. **What this audit does not establish.** It reads what each row says decided it, and a row cannot disclose an influence its author did not notice. The repository's history is the only record of what output actually existed when each row was written, and the audit was not conducted against it. **On §7's removal rule and F6's threshold.** They are two consequences at two scopes and the nesting is deliberate: one flip costs that claim, four cost the frame. The defect was never the nesting — it was that the two sentences had come to range over different sets. Four arms (`naive_retry`, `vasool_ungated`, A1, A5) can flip and count toward F6 while §7's removal sentence, which quantifies over §9's conclusions, says nothing about them; and F2 can flip on attempts-consumed, be removed from the pitch under §7, and stay invisible to F6, which is primary-only. Registering the removal rule as a precondition dissolves that without touching §7: the question stops being *which set is this a conclusion in* and becomes *did the comparison behind this specific claim survive*, which is answerable for any claim from either set. **§7's sentence is deliberately not amended.** "Whatever it says" is its load-bearing half — it forecloses arguing a flipped conclusion back in on its merits — and a rule that survives being restated as a precondition did not need rewriting. Both rules are dormant on current data: 0 of 7 comparisons flipped, so nothing is removed from the pitch and F6 does not fire. | **No** — the relevant output for a change to a disclosure convention is this table, which is not run output; no estimate, interval or verdict informed the standard's wording or the audit. Disclosed for completeness: this row exists because reading the §7 grid produced the two rows above and their Yes marks exposed the conflict, so grid output is upstream of the row's *existence* though not of its *content*. |
| 2026-08-24 | **§10's two knob counts are reconciled and the §7 seed-count row's "24 knobs" is superseded.** That row (2026-08-23) sized the grid at *24 knobs × 4 points plus 3 mix composites, × 9 arms*; it was written before the two exclusions registered later in this same table. The grid actually run reconciles to it as follows: **24 counted knobs − 3 that are not registered parameters at all** (universe epoch, merchant count, consent age — this table's last row flags them *Not swept*, because scaling a calendar anchor or a count by ±50% is meaningless, and they are not in `OUTCOME_PARAMETERS` or `WORLD_PARAMETERS`) **= 21 registered scalars** (§4's 9 outcome parameters and §10's 12 world-shape parameters) **− 1 registered but excluded from the grid** (`retry_success_instrument_dead`; 0.0 × any factor = 0.0, this table's second row) **= 20 swept knobs × 4 points = 80, plus 3 mix composites = 83 configurations.** With §10's equally-powered reference, 84 configurations are run and 83 are serialised; × 9 arms × 200 seeds. **20 knobs and 83 configurations is the operative count** wherever this table or the report card states the grid's size. | **Two counts of one grid in one file is the defect; neither count was wrong when it was written.** The earlier row is not corrected in place, because §10 is append-only — it is superseded here, and the arithmetic is written out step by step rather than asserted, so a reader can reconcile it against `windtunnel/sweeps.py::sweep_configurations` without having to trust either row. **The direction of the discrepancy is stated rather than buried.** The 82-hour estimate that justified §10's 200-seed range was computed against 99 configurations (24 × 4 + 3); the 83 that actually run are about 16% fewer, so that estimate overstated the cost of the alternative it rejected. That is the conservative direction — the seed-range decision would have gone the same way against the smaller grid, and does not need re-making — but a reader comparing the two numbers is entitled to know the estimate ran high rather than low, and to be told so here rather than to derive it. | **No** — the count reproduces from `OUTCOME_PARAMETERS`, `WORLD_PARAMETERS` and `sweep_configurations()` with no run behind it; no estimate, interval or verdict informed it. Disclosed for completeness: 83 was first noticed in the grid's manifest, though it is obtainable from the code alone. |
| 2026-08-24 | **Registered as a known limit, unclosed: there is no CUSTOMER_ACTION counterpart to §2a's two class-keyed claims or to `metrics.py`'s two world-keyed counters.** §2a has eight claims, of which two are class-keyed — no automated action on a `RISK_BLOCK` episode, and no retry on an `INSTRUMENT_DEAD` classification beyond the documented single probe — and both key on `Proposal.failure_class`, the label the arm itself assigned. `windtunnel/metrics.py` adds two world-keyed counters beside them, `instrument_dead_retries_world` and `risk_block_actions_world`, which count the same behaviour against the world's registered class so that an arm declining to classify cannot satisfy §2a vacuously. **CUSTOMER_ACTION has neither.** It is **0.09 of §3d's registered mix** — `payment_cancelled` 0.07 plus `card_number_invalid` 0.02 — and both rows carry `retry_budget = 0` in `vasool/diagnosis/taxonomy.py`. A non-classifying arm retrying those episodes is therefore invisible three times over: to §2a's Proposal-keyed scan, because it labelled the episode TRANSIENT; to `instrument_dead_retries_world`, because the true class is not INSTRUMENT_DEAD; and to `risk_block_actions_world`, for the same reason. Meanwhile the retry is credited with recovery at `retry_success_unpriced_class` = **0.35**, the highest registered retry rate in §4. Roughly **one episode in eleven** gives the baselines retry-sourced recovery on a class the taxonomy prices at zero attempts, with no counter reporting it, while identical behaviour on the other two classes is counted twice. | **The direction is not neutral and is stated plainly: this makes the baselines look better on the primary metric and no worse on any reported guardrail.** `retry_plus_contact` already leads Vasool by 31 percentage points of recovery rate, so the arm most favoured by the gap is the one already winning, and the gap's effect runs the same way as its lead. The asymmetry is in the counters, not in the outcome model: `windtunnel/outcome.py` prices the unpriced classes at 0.35 precisely because only the baselines retry them, and its own comment records that generosity as the deliberate anti-Vasool reading. That reasoning holds. What does not hold is that the generosity is visible in recovery rate while the compliance cost of the retries earning it is visible nowhere — §2a's two class-keyed claims and the two world counters exist exactly to stop that trade being invisible, and they cover two of the three classes where it arises. **Closing it needs a third world-keyed counter in `measure()` and a full re-run of the §7 grid**, because a new counter changes what every shard records and the shards are what the grid is made of. **It is left unclosed for time, not because it is small** — one episode in eleven, on the metric the submission leads with, against the baseline that beats Vasool, is not small, and recording it here as a limit is not the same as having measured it. A reader comparing arms on recovery rate should treat the baselines' figures as containing an unmeasured component of this kind. | **Yes** — the limit itself is structural and derivable from `metrics.py`'s fields and §2a's table with no run at all, but it was found with the grid's output on screen, and the direction-of-effect statement above quotes measured figures (the 31-point lead, and the per-arm counter gaps that showed the two counters agreeing exactly for every classifying arm). Marked Yes under §10's superseding standard rather than argued into a No on the grounds that the structural half came first. |
| 2026-08-25 | **The obligation-loop fix.** `PreDebitNoticeGuard` defect fixed on the deferral path, and the dead loop in `_execute` removed. | Found by A23 in Session 8 — an attack that passed the criterion by being inert. The guard emitted its obligation on a `DEFER` while `_execute` was the only place obligations were honoured, causing a liveness deadlock. 1,353 tests passed with the defect present. | **Yes** — Marked under §10's superseding POST-HOC standard. The defect was found via A23 after evaluating runs. |
| 2026-08-25 | **The re-run, with before and after.** Vasool 0.344341 → 0.490684. F5 gap 19.378 → 4.744 against a threshold of 20, so 75% of the measured "price of the guards" was this defect. F1 vs `retry_plus_contact` −0.310 → −0.164. `naive_retry` −0.122 → +0.025, a flipped conclusion. | §3c requires the fix and the re-run both recorded with the reason. The three arms that did not move are exactly the three with `chain=()`. | **Yes** — The re-run results and comparisons were directly visible. |
| 2026-08-25 | **The stale-shard incident.** The first `make eval` after the fix finished in 5.5s and re-emitted pre-fix rows as a post-fix evaluation. Recorded that the stale shards were preserved, not deleted, and that a fingerprint fix is outstanding. | The resume skips seeds already present, justified by invariant 5 — which holds for a fixed agent and not across a change to one — and a shard carries no fingerprint of the agent that produced it. §3c exists to prevent a silent re-run; this is the mirror, a silent non-run, and only the implausible elapsed time surfaced it. | **Yes** — The elapsed time output and stale shard contents were visible. |
| 2026-08-25 | **A4's exclusion ground is now stale.** Dropped the exclusion of A4 from F6's denominator, restoring it to the set of evaluated arms. | Row 1 excludes A4 from F6's denominator because "its per-seed difference is identically [0, 0]." Post-fix it is +7.29e-05, excludes zero, and survives in most configurations. The original ground is now empirically false. Because finding a new reason to preserve the exclusion would violate the protocol, the exclusion is dropped. | **Yes** — Based directly on seeing A4's post-fix paired difference of +7.29e-05. |
| 2026-08-25 | **A3 at §7's depth.** Registered that §7 cannot speak to A3's recovery-rate comparison either way at this depth. The registered rule is not changed. | A3's reference recovery interval at 200 seeds is [−0.00057, +0.00196]. It includes zero, so `survives()` fails in all 83 configurations because the reference was never conclusive, not because any parameter moved it. This is the hazard the 2026-08-23 seed-count row named: crossing zero from lost power rather than from the parameter. The effect pushes F6 toward firing, which is the conservative direction. Note that F2 still does not fire on attempts-consumed at −0.151, which is the half of F2 carrying the flagship claim. | **Yes** — Made with A3's 200-seed output interval of [−0.00057, +0.00196] visible. |
| 2026-08-28 | **The `CUSTOMER_ACTION` world-keyed counter is reported, and the 2026-08-24 limit above is closed.** `windtunnel/evaluate.py`'s base-protocol aggregation now emits `customer_action_retries_world` beside the two existing world counters, and Exhibit B of the report card renders all three as a table, labelled there as world numbers and explicitly not as §2a ledger scans. **Measured, 1000 seeds, development cohort:** `naive_retry` 66,040, `retry_plus_contact` 66,040, A1 29,672, and **zero** for Vasool, `vasool_ungated`, A2, A3, A4 and A5. | The 2026-08-24 row registered the absence as an open limit and priced closing it at "a third world-keyed counter in `measure()` and a full re-run of the §7 grid, because a new counter changes what every shard records and the shards are what the grid is made of". **That price was correct when the row was written and had already been paid by the time it was read.** The obligation-loop re-run of 2026-08-25 (§9.13) regenerated every shard against a `measure()` that already computed the field, so all 765 shards on disk — 9 base and 756 sweep — carry it, and the closure is one aggregation line over rows that already exist. No seed was re-simulated; `make sweeps` resumed the whole grid in 16.1 seconds. **That fast resume was verified rather than trusted**, because §10's own stale-shard incident four rows above is exactly what a 16-second `make sweeps` otherwise looks like, and a shard still carries no fingerprint of the agent that produced it. Two checks, both before the aggregation was believed: twelve (arm, seed) rows across four arms were recomputed from the current working tree and compared field-by-field against their cached counterparts, all twelve identical; and the regenerated artifact was diffed against its predecessor, giving nine added keys and **no other changed value anywhere in the file** — every interval, every survival boolean and every F verdict byte-identical. **No conclusion moves.** The counter reports; it enters no criterion. What it changes is that the 2026-08-24 row's direction-of-effect claim is now a measured quantity rather than an argued one: the two baselines spend 66,040 retries each on a class the taxonomy prices at zero attempts, and `retry_plus_contact` — the arm ahead of Vasool by 16.4 points of recovery rate — is one of them. **The zeros are not all the same fact, and the shared one is the informative one.** Vasool and `vasool_ungated` both report zero, and `vasool_ungated` runs with `chain=()` — so that zero is not the guards refusing a retry, it is the classifier never proposing one, both `CUSTOMER_ACTION` rows carrying `retry_budget = 0` in `vasool/diagnosis/taxonomy.py`. The counter therefore isolates classification and not gating, which is what makes A1's 29,672 the interpretable figure: A1 is the arm that routes every reason through one generic row. | **Yes** — the per-arm values were computed from the cached shards and read before this row was written. The limit it closes was itself marked Yes. |
| 2026-08-29 | **The red-team suite was run in full for the first time on record, and taxonomy §9.12's four demonstrations are closed.** Result: **18 of 22 attacks survive** (`out/adversary/redteam.json`, `as_registered: true`). **A15, A16, A18 and A19 all survive**, so the queued-proposal / deferral defect §9.12 describes is closed on every demonstration it registered. Four failures remain and are named: **A01** out-of-band payment mid-ladder (1 action after money arrived), **A07** one human, two customer ids (4 contacts in 7d against a cap of 3), **A08** contact window in the wrong timezone (1 contact at 22:30 customer-local), **A09** a message to a DND-listed customer. | §9.12's "Fixed 2026-08-25" paragraph already asserted that the four were "registered as surviving controls and exercised by the red-team suite", and **no run of that suite was recorded anywhere in the repository** — the claim rested on the fixes having been written, not on the criterion having been applied to them. That is the distinction `windtunnel/adversary/criterion.py` exists to enforce: `judge()` is the only thing that can produce a verdict, and it scans the ledger the way §2a scans rather than trusting that a guard returned BLOCKED. An assertion about surviving controls is subject to the same rule. The run now exists and the artifact is on disk. **This also contradicts a published figure.** `README.md` states "13 attacks were structurally blocked, while 9 remain open as known limitations" and lists "+5 other structural failures" beyond the four it names. No run supports either number: the count is 18 and 4, and the four named in the README — A01, A07, A08, A09 — are the complete set, with no fifth, let alone five more. The README is not amended by this row; the divergence is recorded here so the correction is dated. | **Yes** — written after running the suite and reading its output. |
| 2026-08-29 | **Shard provenance established directly, and one stale field found.** The 765 shards behind the current artifact were written 2026-08-25 10:03–18:29; `vasool/ledger/receipts.py` was last modified that day at 21:00 and `vasool/policy/machine.py` at 21:08. **The shards therefore predate the current agent code by roughly three hours**, and the 16.1-second resume recorded in the row of 2026-08-28 could not on its own distinguish that from a stale re-emission. It was resolved by recomputation, not by inference from matching values: the entire base protocol was re-run from the working tree into a scratch directory — 9 arms × 1000 seeds — and compared field-by-field against the cached shards. **9,000 rows, 207,000 field comparisons, byte-identical**, and a manifest built from the fresh shards has identical `per_arm`, `paired_vs_vasool`, `pass_k` and F1–F5. §7's grid was sampled at 360 rows across 30 of its 84 configurations. **One field does not reproduce.** In **83 of the 84 sweep configurations** the cached `safety` block records `receipt_chain_verifies` failing on one receipt, which drives `safety_holds` to false for Vasool in every one of them; recomputed today the same rows verify cleanly. The single exception is `reference`, written 10:21:48 alongside the base shards. So chain verification regressed sometime between 10:21 and the first swept configuration at 12:15, and was repaired by the 21:00 edit to `receipts.py`. | §3c exists to prevent a silent re-run and the row of 2026-08-25 records the mirror, a silent non-run. A resume that finishes in seconds is indistinguishable from either unless the shards are checked against the code that is supposed to have produced them, and a shard still carries no fingerprint. Recomputation is the only evidence available and it is now on the record. **The stale field changes no published number, and the reason is structural rather than lucky.** §7's manifest blocks carry `kind`, `target`, `factor` and per-arm `{survives, interval}` and nothing else — no safety data reaches the manifest from the grid at all — and every §7-relevant metric reproduced exactly in the sample, including `recovery_rate`, on which survival and therefore F6 are computed. The §2a predicate the submission rests on is reported from the **base** shards, which reproduce completely. **What it does mean is that the sweep shards must not be used for any safety statement**, and that the grid's `safety` column is evidence of a bug that existed for about nine hours rather than evidence about the agent. Re-running the grid to clean it costs the ~9 hours §10's 2026-08-23 row prices and buys no reported number; it is left stale and labelled here instead. | **Yes** — written after reading both the recomputation and the shard comparison. |
| 2026-08-29 | **Reconciliation: the re-run row of 2026-08-25 does not agree with the artifact on disk, and the artifact is the authoritative one.** That row records Vasool's mean recovery rate as **0.490684** and F5's gap as **4.744**; `out/development/evaluation.json` carries **0.4906981214797104** and **4.742469706909041**. Deltas: +1.41e-05 on the primary, −0.0015 absolute percentage points on F5. The dated row is not amended — this document is append-only and a row records what was true when it was written. | The current figures are reproducible and the earlier ones are not: the shards on disk regenerate byte-identically from the working tree (see the provenance row above), while **no shard set that aggregates to 0.490684 exists anywhere in `out/`**. The 2026-08-25 row therefore describes a run whose shards were subsequently replaced — consistent with that day's sequence, in which the stale-shard incident forced a re-run and the guard fixes behind A18 and A19 landed at 09:04, before the surviving shards were written at 10:03. **That sequence is the likely explanation and it is not established here**; the working tree cannot reproduce a shard set it no longer has, and inventing a cause for a 1.4e-05 discrepancy would be worse than recording it unexplained. **Nothing turns on it.** F5's verdict is 4.74 against a threshold of 20 on either figure; F1's point estimate and direction are unchanged at three decimal places (−0.164, Vasool behind); no criterion's `fired` value moves. The reason to record it at all is that two documents in this repository state different values for the same headline number, and a reader finding both is entitled to know which one the artifact supports. | **Yes** — written with both figures visible. |
| 2026-08-29 | **The holdout was unsealed and evaluated. Once.** §3c's sealed 60% was run with the registered phrase across all 9 arms × 1,000 seeds; results in `out/holdout/evaluation.json`. **Every registered conclusion replicates.** Vasool **48.92%** against development's 49.07%; `retry_plus_contact` **65.24%** against 65.42%. F1's paired difference is **−16.311pp** [−16.454, −16.166] against development's −16.353pp [−16.540, −16.166] — same sign, same magnitude, interval still excluding zero on the baseline's side. F5's gap **4.719pp** against 4.742pp, both far under the 20pp threshold. The §2a predicate holds on **1,000 of 1,000** holdout seeds with `pass^k` = 1.0 at every registered k, and `retry_plus_contact` and `vasool_ungated` hold it on **0 of 1,000**, exactly as on development. **F1–F5 return identical verdicts on both cohorts; none fired.** No arm moved more than 0.19pp. | §3c registers the holdout as evaluated **once**, and this is that once — recorded here before any figure from it was quoted anywhere else, which is the sequencing the section exists to enforce. **What this does and does not establish.** It does not validate the outcome model: both cohorts are drawn from the same registered universe, so a wrong parameter is wrong in both. What it rules out is the failure §3c was actually written against — that thresholds were tuned against visible numbers until the result appeared. A taxonomy fitted to the development set would not reproduce its own effect sizes to within two hundredths of a percentage point on customers it had never been measured on. **Two limits on this row, stated rather than left implicit.** (i) **F6 is not evaluated on the holdout.** §7's grid is 83 configurations × 200 seeds and was registered against the development cohort; running it again here would cost ~9 hours and re-open a question §7 already answered. The holdout manifest reports `F6: null`, which is the honest value for a criterion this run did not evaluate. (ii) **F7 reports `null` in the holdout manifest** because that run began minutes before the amendment two rows below wired F7 to the determinism result; the answer is nonetheless in the same file, at `determinism.identical: true`. **The holdout was deliberately not re-run to fix the cosmetics of one field** — a second execution is precisely what §3c forbids, and a null beside a true answer is a smaller defect than an unsealed cohort evaluated twice. | **No** — this is the registered protocol executed as written. The run was launched before its results existed, on a criterion set fixed since 2026-08-24, and nothing in this document was altered in the light of what it returned. |
| 2026-08-29 | **§10's report card asked for an "honest exceptions" block and it was never built; it is added now, from the shards already on disk.** The spec's report-card sketch specifies unrecovered episodes split by reason, with `RISK_BLOCK -> human queue` marked *correct behaviour, not a failure*. The artifact reported only `recovery_rate_mean`, so **every one of Vasool's 180,723 non-recoveries rendered as an undifferentiated failure**, including the 29,118 the guards correctly refused. `per_arm.*.closure` now carries the five-way partition `{episodes, recovered, blocked, escalated, exhausted, awaiting}` for all 9 arms. Vasool: **174,103 recovered, 29,118 blocked, 13,014 escalated, 0 exhausted, 138,591 awaiting**. | **The fifth bucket is a subtraction, not a new measurement, and that is what makes this closeable without a re-run.** The four terminal states are absorbing and BLOCKED/ESCALATED/RECOVERED receipts are written at the terminal transition, so the three receipt-derived counters are disjoint from one another and from `recovered` — checked over 25 seeds, **zero overlap in all three pairs**, and the per-seed counts reconcile exactly against `final_states`. So `awaiting = episodes - recovered - blocked - escalated - exhausted`, over fields every shard has always carried. `windtunnel/evaluate.py::_base_protocol` now emits the block natively; `tools/backfill_closure.py` merged it into the existing manifest, and **refuses to write unless the shards reproduce the manifest** on `recovery_rate_mean`, `recovered_paise_total` and `seeds` for all 9 arms — the INC-003 guard, since a merge from a foreign shard set is exactly that failure. All 9 reproduced. Nothing else in the file changed, verified by comparing the manifest before and after with the new blocks stripped. **The distinction this makes reportable is material and cuts against the flattering reading**: of the 180,723 that did not recover, **76.7% are `awaiting` — right-censored at the horizon, not failed** — and only 42,132 are terminal non-recoveries. A reader who subtracts blocked+escalated from non-recoveries and finds 138,591 unexplained is entitled to that number, so the exhibit names it rather than folding it into either side. | **Yes** — the aggregation was written after the base protocol had been run, and the figures above were visible while this row was written. It changes no registered metric, criterion or verdict: `recovery_rate_mean` and every F1–F7 threshold are computed exactly as before, and `closure` is reported beside them, not into them. |
| 2026-08-30 | **Adversary attack A08 closed. 18 of 22 becomes 19 of 22.** `ContactWindowGuard` evaluated the RBI contact window in IST, the merchant's zone, so a customer elsewhere was protected by our clock rather than their own; A08 demonstrated a message landing at 22:30 customer-local. `PolicyFacts.customer_zone` was added, the guard now defers into the customer's own window, and `windtunnel/adversary/arena.py` plumbs the `Person.zone` the world already knew. A08's registered expectation moves **FAILS → SURVIVES** and it comes off `test_the_known_open_failures_still_fail`. | **The closure is registered here because the harness refused it twice first, and both refusals are the point.** (i) A *fixed* attack breaks the build exactly as a broken one does — `A08 registered fails, actually survived` — because the expectation is registered rather than assumed, which is what stops a stale list of known failures rotting into decoration. (ii) The first version of the fix made A08 pass with **zero receipts**: deferring into New York's morning pushed the message past the end of the scene, so *no contact outside the window* held because there was no contact at all. `tests/adversary/test_attacks.py::test_every_attack_ran_against_a_ledger_that_exists` exists for exactly that and caught it; the scene now runs to 21:00 IST and the message lands. **No registered metric moves.** No customer the universe builds carries a timezone, so the guard falls back to IST and behaves identically — established by recomputation rather than assertion: 54 (arm, seed) rows across all nine arms, **1,350 field comparisons, byte-identical** to the shards on disk. `evaluation.json` is untouched and nothing was re-run to make this fit. | **Yes** — the fix was made after the evaluation had been run and its results published. It changes no evaluated number, and that claim is checkable from the recomputation above rather than taken on trust. |

Probabilities clamp to [0,1] under sweep, so +50% on 0.97 registers as 1.0
rather than 1.455.

---

## 11. What this evaluation will not claim

- That Vasool would recover X% of real failed payments. It measures a model.
- That the failure-reason mix reflects any real merchant's traffic.
- That the compliance mapping constitutes legal advice, or that the statute
  citations in the guards have been reviewed by anyone qualified.
- That the LLM comparison (Session 7) generalises beyond the ten reasons in
  `docs/taxonomy.md`, nine of which are documentation-derived stubs.
- That `pass^k` over simulated worlds predicts production reliability. It
  bounds the *implementation* against the worlds this simulator can generate,
  which is not the same set as the worlds that exist.
- That time-to-recovery is the time a real customer would take. It measures
  the agent's own scheduling and excludes customer response latency, which is
  not modelled: a settlement lands in the same tick as the action that earned
  it. No click-delay parameter is registered in §4, and inventing one would be
  a parameter this protocol never agreed to — so the metric is a statement
  about when the agent acted, not about when money would really arrive.

---

*Committed and tagged before the simulator existed. Any number appearing
anywhere in this repository that is not derivable from this protocol, on the
registered seeds, is not a result.*